# web

Web interface layer for this repository. Optional — add only when direct programmatic
access to the domain computer is insufficient for the intended use.

See [web conventions](../.yidam/.vendor/prelude/guidelines/directories.md#web) for what belongs here.

<!-- TEMPLATE
Describe the intended web interface, if applicable:
- What does a user do here? (Browse corpus, issue retrieval queries, visualize the graph,
  surface synthesis, submit documents, explore hypotheses?)
- What is the primary data source? (Corpus directly, or a bundled export feed with a
  contract version?)
- Public-facing or internal tooling?
- Deployment target (Cloudflare Pages, Vercel, self-hosted)?

If web/ is not applicable at genesis, say so and describe what would trigger adding it.
-->

## Bundle status

<!-- REGEN: yidam bundle-status
Regenerated by: `yidam bundle-status`
Fields: bundle contract version, feed list, last export timestamp, node counts per feed,
        deployment target, last deploy status.
-->
_Run `yidam bundle-status` to populate._
<!-- /REGEN -->
