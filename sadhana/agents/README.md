# agents

Domain-specific agent definitions. Generic agents inherited from yidam live in
[`.yidam/.vendor/prelude/`](../.yidam/.vendor/prelude/); agents whose purpose is specific to this domain live here.

<!-- TEMPLATE
List the named agents expected to operate in this domain, with a one-line description
of each. The bootstrap will create stub definition files for them.

Example:
- **field-investigator** — explores the corpus for a given site or hypothesis thread
- **synthesis-agent** — links findings across inquiry threads and proposes new edges
- **extraction-agent** — extracts structured data from primary sources into corpus nodes

If no domain-specific agents are needed at genesis, leave this blank and add agents as
phases define the need.
-->

## Registry

<!-- REGEN: yidam agents-index
Regenerated by: `yidam agents-index`
Fields per agent: filename, name, description, skills it invokes, model preference.
-->
_Run `yidam agents-index` to populate._
<!-- /REGEN -->
