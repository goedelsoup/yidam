# packages

Other-language packages in the same domain computer layer as [`crates/`](../crates/).
Use this directory when ecosystem access, ML frameworks, or upstream SDKs are better
served by Python, TypeScript, or another runtime than Rust.

The connector/calculator/feature-engineering distinction applies here equally — see
[packages conventions](../.yidam/.vendor/prelude/guidelines/directories.md#packages).

<!-- TEMPLATE
Describe which domain computer capabilities belong here rather than in crates/. Common
reasons:
- Embedding model SDKs and ML pipelines (Python)
- Geospatial libraries with no mature Rust equivalent
- Statistical packages (SciPy, statsmodels)
- Frontend retrieval or visualization integrations (TypeScript)

If the domain computer is Rust-only at genesis, leave this empty and add packages when
the need is concrete.
-->

## Packages

<!-- REGEN: yidam packages-index
Regenerated by: `yidam packages-index`
Fields per package: name, the capability that runs it and its kind (connector/calculator),
                    description.
The capability column is read from `.yidam/capabilities.toml` and is an em dash where
nothing declares the package — see `crates/README.md` for why that distinction is the
point of the column.
-->
_Run `yidam packages-index` to populate._
<!-- /REGEN -->
