---
kind: axiom
---

# Object

The thing whose history is being established: one accessioned item, identified by the number
the holding institution gave it rather than by its title. Titles change with attribution and
with translation; the accession number is what the institution's own records key on.

Everything else in this corpus exists to say where this object was, and when, and on whose
authority the answer rests.
