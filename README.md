# <!-- TEMPLATE: repository name (the domain slug, e.g. "causal-inference") -->

<!-- TEMPLATE
One or two sentences: what domain this repository investigates and what the central
question is. Write it as a research instrument description, not a project description.

Example: "This repository tracks the hydrological impact of industrial cooling facilities
on the Shawnee River basin. The central question is whether current discharge permits
are consistent with low-flow assimilative capacity."
-->

## Navigation

| Directory | Contents |
|---|---|
| [`corpus/`](corpus/) | Knowledge nodes — concepts, relations, artifacts, open questions |
| [`catalog/`](catalog/) | Data source registry — provenance for external sources |
| [`agents/`](agents/) | Agent definitions specific to this domain |
| [`skills/`](skills/) | Domain-specific skills available to agents |
| [`crates/`](crates/) | Rust domain computer — connectors, calculators, feature engineering, index |
| [`packages/`](packages/) | Other-language domain computer packages |
| [`web/`](web/) | Web interface layer (if applicable) |
| [`sangha/`](sangha/) | Collective resolution protocol |

Foundational model: [`prelude/`](prelude/) — read before acting.

## Current state

<!-- REGEN: yidam status
Regenerated by: `yidam status`
Fields: corpus node count, open question count, catalog source count, index freshness,
        active phase branches, last genesis commit date.
-->
_Run `yidam status` to populate._
<!-- /REGEN -->

## Open questions

<!-- REGEN: yidam open-questions
Regenerated by: `yidam open-questions`
Lists all corpus nodes whose title begins with `?` or whose content contains `[open]` claims.
-->
_Run `yidam open-questions` to populate._
<!-- /REGEN -->

## Example Bootstrap

```
Read BOOTSTRAP.md — this is your entry prompt. You are initializing a new yidam-derived repository.

Before doing anything else, check whether `samudaya/` exists and read every file in it (excluding `samudaya/examples/`). Then internalize the full prelude under `prelude/`. Only after that, begin the ontology-discovery dialogue.

Do not scaffold, create files, or commit anything until we have confirmed the ontology sketch together.
```
