---
// Suppress Starlight's auto-rendered page title.
// The H1 heading in each source file serves as the visual heading,
// so it travels correctly whether the file is read as plain Markdown or
// rendered through this site. The frontmatter title (extracted from H1 by
// the docs-loader) still drives the sidebar label and browser tab title.
---
