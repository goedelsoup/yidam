function = "extract_links"
description = "empty content yields no links"

# No links. Must be a top-level `expected = []` declared before [input] —
# a bare [[expected]] would create an array with one empty element.
expected = []

[input]
content = ""
