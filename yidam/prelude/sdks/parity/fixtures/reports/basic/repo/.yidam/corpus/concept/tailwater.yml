class: concept
label: Tailwater
description: >-
  Water downstream of a structure. The control node — it trips nothing, including when it
  names the vocabulary rather than using it: no claim here is `[open]`, and nobody has
  written `[verified]` against this node. Both are mentions because the sentences negate and
  report them, not because they are in code — the backticks decide nothing, and the counted
  claim on [the gauge](../gauge/riffle-station.yml) is backticked too.
links:
  - target: ../concept/low-flow.yml
    relationship: relates-to
  - target: ../concept/mixing-zone.yml
    relationship: relates-to
