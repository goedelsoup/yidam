class: concept
label: Low flow
description: A discharge regime. Carries one deliberately broken edge, and draws on
  [the stage–discharge relation](../../catalog/stage-discharge.md) — a citation the
  entry's own `used-by` list does record.
properties:
  claim_tag: open
links:
  - target: ../concept/assimilative-capacit.yml
    relationship: depends-on
