---
title: Operating record for a regulated reach
url: https://example.invalid/operating-record
retrieved: 2020-01-01
ttl_days: 1
---

# Operating record

An illustrative source, expired by construction. `retrieved:` is 2020 and `ttl_days:` is 1,
so it is past its TTL on any day this suite is run and will stay past it — the same device
`corpus-dated/` uses with nineteenth-century dates, and for the same reason: a fixture whose
finding depends on the day it runs is a fixture whose assertions move.

It reproduces no observation from any real station, and the URL resolves to nothing.
