[package]
name = "yidam-domain-energy"
version = "0.1.0"
edition = "2021"
description = "Yidam energy domain — classical mechanics calculations"

[dev-dependencies]
# The fixture loader, and the emptiness guard that lives inside it (#686).
yidam-domain-testkit = { path = "../../parity/testkit" }
