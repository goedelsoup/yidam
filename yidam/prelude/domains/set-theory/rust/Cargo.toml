[package]
name = "yidam-domain-set-theory"
version = "0.1.0"
edition = "2021"
description = "Yidam set theory domain — pure set operations"

[dev-dependencies]
# The fixture loader, and the emptiness guard that lives inside it (#686).
yidam-domain-testkit = { path = "../../parity/testkit" }
