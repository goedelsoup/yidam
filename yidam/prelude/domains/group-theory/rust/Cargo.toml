[package]
name = "yidam-domain-group-theory"
version = "0.1.0"
edition = "2021"
description = "Yidam group theory domain — finite group arithmetic"

[dev-dependencies]
# The fixture loader, and the emptiness guard that lives inside it (#686).
yidam-domain-testkit = { path = "../../parity/testkit" }
