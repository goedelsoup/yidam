[package]
name = "yidam-domain-economics"
version = "0.1.0"
edition = "2021"
description = "Yidam economics domain — introductory economic identities"

[dev-dependencies]
# The fixture loader, and the emptiness guard that lives inside it (#686).
yidam-domain-testkit = { path = "../../parity/testkit" }
