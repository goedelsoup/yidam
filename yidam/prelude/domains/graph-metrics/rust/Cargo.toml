[package]
name = "yidam-domain-graph-metrics"
version = "0.1.0"
edition = "2021"
description = "Yidam graph metrics domain — pure graph calculation functions"

[dev-dependencies]
# The fixture loader, and the emptiness guard that lives inside it (#686).
yidam-domain-testkit = { path = "../../parity/testkit" }
