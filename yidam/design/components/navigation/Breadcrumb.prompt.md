Hierarchical path navigation. Current item (last) is non-linked, font-weight 500. Pass `mono` for corpus node paths.

```jsx
<Breadcrumb items={[{ label: 'corpus', href: '/corpus' }, { label: 'hydrology' }, { label: 'watershed-boundaries.md' }]} mono />
<Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'Graph', href: '/graph' }, { label: 'Node detail' }]} />
```
