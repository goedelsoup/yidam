{
  "name": "corpus-web",
  "private": true,
  "version": "0.1.0",
  "description": "Corpus semantic search — offline WASM shell",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "apache-arrow": "^17.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
